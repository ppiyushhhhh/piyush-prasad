# Standalone Styles

`portfolio-template.css` is a self-contained stylesheet that pairs with
`docs/examples/portfolio-template.html`.

- Not imported by the app.
- No Tailwind, no PostCSS pipeline required.
- Uses CSS custom properties for theming and a `.theme-dark` class hook.

Include it via a plain `<link rel="stylesheet">` tag from any HTML file.
